# ============================================================================
# agents/__init__.py
# Agent 子包：定义 Agent 公共接口与 ReAct 循环实现。
#
# 设计意图（对标 DSH）：
#   核心哲学是"一切皆可替换"——Agent 循环本身也是一个插件。
#   agent_interface 定义 Agent 抽象契约，agent_loop_plugin 提供默认的
#   Function-Calling 循环实现。任何新的 Agent 只需实现 Agent 接口并注册，
#   即可替换默认循环，而无需改动其他任何代码。
# ============================================================================
