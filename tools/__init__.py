# ============================================================================
# tools/__init__.py
# 示例工具子包：提供 Agent 可调用的具体工具插件。
#
# 设计意图：
#   这些工具通过 ToolsPlugin 提供的 'tools' 服务注册到工具注册表，
#   再注入 systemPrompt，使 Agent（LLM）能够调用它们完成任务。
#   新增工具只需在 apply 中调用 tools.register_fn 即可，无需改动核心。
# ============================================================================
